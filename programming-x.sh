#!/bin/sh
# NVM
# export NVM_DIR="$HOME/.nvm"
# [ -s "$NVM_DIR/nvm.sh" ] && source "$NVM_DIR/nvm.sh"  # This loads nvm
# [ -s "$NVM_DIR/bash_completion" ] && source "$NVM_DIR/bash_completion"  # This loads nvm bash_completion

# RVM
# export PATH="$PATH:$HOME/.rvm/bin" # Add RVM to PATH for scripting.
# [[ -s "$HOME/.rvm/scripts/rvm" ]] && source "$HOME/.rvm/scripts/rvm" # Load RVM into a shell session *as a function

# PYTHONZ
# [[ -s $HOME/.pythonz/etc/bashrc ]] && source $HOME/.pythonz/etc/bashrc

# GVM
# [[ -s "/home/josh/.gvm/scripts/gvm" ]] && source "/home/josh/.gvm/scripts/gvm"

# LUAVER
# [ -s ~/.luaver/luaver ] && source ~/.luaver/luaver
# [ -s ~/.luaver/completions/luaver.bash ] && source ~/.luaver/completions/luaver.bash

# PERLBREW
# export PERLBREW_ROOT=~/.perlbrew
# [ -s ~/.perlbrew/etc/bashrc ] && source ~/.perlbrew/etc/bashrc

# PHPBREW
# [ -s ~/.phpbrew/bashrc ] && source /home/josh/.phpbrew/bashrc
